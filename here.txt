- grouping and separation is as follows
    main -> will just execute
    player -> just the player
    map -> map generation, bliting
    objects -> all items
    server -> will host the server
    game -> basically the client

- pygame.Surface.subsurface for spritesheet
- server
